# 경주한적 커뮤니티 + 혼잡도 + 코스추천 통합 변경사항

기준 원본: `backend(7).zip`

## 그대로 유지한 핵심 규칙
1. 혼잡도 V2.4.4 가중치
   - baseline 35%
   - time 15%
   - NAVER popularity 6%
   - NAVER momentum 4%
   - weather 5%
   - official 25%
   - regional 최대 10%
   - 마지막 capacity factor
2. 장소 추천 점수
   - 한적함 45%
   - 취향 30%
   - 출발지 거리 25%
3. 맛집/카페는 관광지 취향 점수와 분리
4. 꼭 포함 장소는 일반 후보 필터보다 우선
5. 탐색 반경을 전역 하드필터로 사용하지 않음

## 새 통합 로직
### 커뮤니티 현장 제보
- 최근 2시간 `live` 글만 코스 보조 신호로 사용
- 오래된 제보일수록 선형으로 신선도 감소
- 1건 최대 8%, 2건 최대 12%, 3건 최대 16%, 4건 이상 최대 20%
- 신선도에 따라 실제 영향은 이보다 더 작아질 수 있음

### 점수 분리
- `congestion_score`: V2.4.4 원본
- `community_congestion_score`: 최근 커뮤니티 제보 집계
- `routing_congestion_score`: 코스 생성용 보정값

즉 커뮤니티 제보가 V2.4.4 결과를 덮어쓰지 않습니다.

### 코스 생성
- 45/30/25 비율은 유지
- 45% 한적함 부분에서 `routing_congestion_score` 사용
- 혼잡도 우선 후보 선별에도 같은 값 사용
- 맛집/카페의 한적함 평가에도 같은 값 사용
- 여행 중 다음 장소 재조정에도 같은 값 사용

### 탐색 반경
- 사용자 UI에서 제거된 `radius_km`를 코스 후반 장거리 이동의 하드 컷으로 다시 쓰지 않도록 수정
- 비정상적인 장거리 점프만 이동수단별 안전 상한으로 방지
  - 도보 6km
  - 대중교통 14km
  - 자동차 20km

### 커뮤니티 코스 따라가기
`course-copy` 응답에 기존 코스의 관광지 이름, 맛집/카페 포함 여부, 추천 시간, `/routes/recommend` 재요청용 패치를 함께 반환합니다.

이 패치에 현재 출발 좌표/이동수단을 넣어 다시 추천하면 기존 관광지를 필수 장소로 보존하면서 현재 혼잡도와 이동조건을 다시 계산할 수 있습니다.
