# 경주한적 커뮤니티·혼잡도·코스·장소카드 통합 변경

## 유지 규칙
- V2.4.4 혼잡도 가중치 유지
- 코스 장소 점수: 한적함 45% + 취향 30% + 출발지 거리 25% 유지
- 커뮤니티 현장 제보는 원본 혼잡도를 덮어쓰지 않고 routing congestion에만 제한적으로 반영
- 탐색 반경을 관광지 하드필터로 사용하지 않음

## 이번 추가 연결
1. 커뮤니티 `지금 여기` 제보 -> 장소별 최근 현장 혼잡 요약
2. 장소 상세 -> 앱 예상 혼잡도와 커뮤니티 체감 혼잡도를 별도로 표시
3. 커뮤니티 `이 코스 따라가기` -> 게시 당시 스냅샷 복사 대신 현재 위치에서 `/routes/recommend` 재호출
4. 재추천 시 원래 관광지들을 필수 장소로 넘기고, 원래 코스의 맛집/카페 포함 여부를 유지
5. 현재 V2.4.4 + 최근 현장 제보를 다시 계산해 코스 구성

## 장소 카드/상세
- 카드 오른쪽 아래 시간은 `recommended_time`이 아니라 `operating_hours` 기반 라벨만 표시
- 운영시간을 확인하지 못하면 임의 시간을 만들지 않고 `운영시간 확인` 표시
- 관광지 소개는 단순 유형 문장으로 생성하지 않음
- TourAPI/공식 홈페이지/NAVER에서 실제 소개 문장을 확인했을 때 표시
- 실제 소개를 확보하지 못하면 프론트가 미확인 상태를 명시
- 공식 홈페이지를 관련 콘텐츠 목록에도 포함
- 관련 콘텐츠는 블로그 / 영상 / 웹사이트로 구분 가능

## 홈
- `지금 덜 붐비는 장소`에서 음식점/카페(contentTypeId 39) 제외
- 관광지(12), 문화시설(14), 여행코스(25), 관광 목적 레포츠(28) 중심
- 맛집/카페는 검색·지도·코스에서 사용 가능
